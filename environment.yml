name: dataseek
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - biopython
  - requests
  - pandas
  - openpyxl
  - pip
  - pip:
    - xenaPython
  - pytest
  - pytest-mock
  - responses
